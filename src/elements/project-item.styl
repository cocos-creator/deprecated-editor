:host {
    display: block

    font-size: 12px
    margin-left: 10px

    .item {
        display: inline-block

        font-family: Helvetica,Arial,sans-serif
        color: #aaa
        cursor: default 

        .type-icon {
            color: #09f
            margin-right: 2px
        }

        .fold-icon {
        }
    }
}
