:host {
    display: flex
    flex-direction: row
    flex-wrap: nowrap     
    align-items: center

    font-size: 12px

    font-family: Helvetica,Arial,sans-serif
    color: #aaa
    cursor: default 

    .icon {
        color: #09f
        margin-right: 5px
    }
}
